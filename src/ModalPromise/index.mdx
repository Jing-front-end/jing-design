---
name: ModalPromise
route: /ModalPromise
menu: 操作反馈
---

import { Playground } from 'docz';
import Modal from './index.tsx';
import Button from '../Button/index.ts';
import './index.less';


# ModalPromise 模态框

## 用法
<Playground>
  {()=>{
    const Temp = () => {
      const [isShow, setIsShow] = React.useState(false);

      function Demo(props) {
        return (
            <div>
              <Modal.Header> 
                测试111
              </Modal.Header>
              <Modal.Body>
                <div>测试1111</div>
                <div>测试</div>
                <div>测试</div>
              </Modal.Body>
              <Modal.Footer></Modal.Footer>
            </div>
        )
    }

      return (
          <div>
            <Button.Group>
              <Button.Self color="blue" onClick={()=>{
                  {/* setIsShow(true); */}
                  {/* Modal.Dialog(Demo).then((res) => {
                    console.log(res);
                  }, (res) => {
                      console.log(res);
                  }); */}
                  Modal.Confirm().then((res) => {
                    console.log(res);
                  }, (res) => {
                      console.log(res);
                  })
                }} primary>确认</Button.Self>
            </Button.Group>
            {/* <div>
              <Modal.Header> 
                测试
              </Modal.Header>
              <Modal.Body>
                <div>测试</div>
                <div>测试</div>
                <div>测试</div>
              </Modal.Body>
              <Modal.Footer></Modal.Footer>
            </div> */}
          </div>
      );
    };

    return <Temp/>;
  }}
</Playground>



## API
| 参数 | 说明 | 类型 | 可选值 | 默认值 |
| :--- | :--- | :---- | :--- | :---- |
|   title  |      标题     | String |  - |  -
| content  |      内容     | String |  - |  -
|cancelText|  取消按钮文本  | String |  - |  取消
|confirmText|  确认按钮文本  | String |  - |  确认
|closeOnClickOverlay| 点击浮层的时候关闭模态框 | Boolean | - | true
| Backdrop | 是否显示背景 | Boolean | `true/false` | true |
| size | 模态框尺寸 | String | `sm/md/lg` | md |
| maxHigher | 是否计算模态框最大高度 | Boolean | `true/false` | true | 



## 事件
| 事件名称 | 说明 | 返回参数 |
| :--- | :--- | :---- |
| onClose | 关闭事件 | 
| onCancel | 取消事件 | 
| onConfirm | 确认事件 |


