---
name: List
route: /List
---

import { Playground } from 'docz';
import List from './index.ts';
import Input from '../Input';

# List 列表大组件

此组件用于各类（混合列表）展示的场景，如：多种类型的交互在同一个列表内，即有单选，又有复选，或者点击跳转等情况；

包括但不限于（单选，复选，点击跳转，银行卡，纯文字，输入框，图标）等；

## 一般用法

<Playground>
  {() => {
    const Temp = () => {
      const [inputValue, setIsInputValue] = React.useState('');
      const [inputValue1, setIsInputValue1] = React.useState('');
      return (
        <List> 我是一个普通的列表 </List>
      );
    };
    return <Temp />;
  }}
</Playground>

## 普通列表的自定义用法

<Playground>
  {() => {
    const Temp = () => {
      const [inputValue, setIsInputValue] = React.useState('');
      const [inputValue1, setIsInputValue1] = React.useState('');
      return (
        <List style={{ justifyContent: 'space-between' }}>
            <div>我是左边</div>
            <div>我是右边</div>
        </List>
      );
    };
    return <Temp />;
  }}
</Playground>

List组件提供容器，其内部内容可以自定义，style，class都可扩展


## 普通列表与其他组件的结合

<Playground>
  {() => {
    const Temp = () => {
      const [inputValue, setIsInputValue] = React.useState('');
      const [inputValue1, setIsInputValue1] = React.useState('');
      return (
        <List>
            <Input.Password 
                groupStyle={{width: '100%' }} 
                style={{ border: 0, padding: 0 }}/>
        </List>
      );
    };
    return <Temp />;
  }}
</Playground>

## 内置列表——一行两列

内置列表均是 外层 group 组件，内层为 写好的样子（一行双列，链接模式，Table模式），

使用者只需填入数据即可

## 普通列表与其他组件的结合

<Playground>
  {() => {
    const Temp = () => {
      const [inputValue, setIsInputValue] = React.useState('');
      const [inputValue1, setIsInputValue1] = React.useState('');
      return (
        <List.Link onLink={(e) => {
            alert('点击我')  
        }}>
            我是一个有链接的列表
        </List.Link>
      );
    };
    return <Temp />;
  }}
</Playground>