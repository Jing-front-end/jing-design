---
name: WhiteSpace
route: /WhiteSpace
menu: 视图组件
---

import { Playground } from 'docz';
import WhiteSpace from './index.tsx';

import './index.less';

## 上下留白


## 使用
<Playground>
  {
    () => {
      const WhiteSpaceExample = () => {
      const PlaceHolder = () => (
        <div className='example-placeholder'>Block</div>
      );
       return (
          <div>
          <WhiteSpace size="xs" />
          <PlaceHolder />
          <WhiteSpace size="sm" />
          <PlaceHolder />
          <WhiteSpace size="md" />
          <PlaceHolder />
          <WhiteSpace size="lg" />
          <PlaceHolder />
           <WhiteSpace size="xl" />
        </div>
       )
      }
      return <WhiteSpaceExample />
    }
  }
</Playground>

## 参数
| 参数 | 说明 | 类型 | 可选值 | 默认值 |
| :--- | :--- | :--- | :---- | :--- |
| size | 上下留白的边距 | String | `xs`, `sm`, `md`, `lg`, `xl` | `md` |