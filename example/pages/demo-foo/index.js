import React from 'react';
import { Button } from '../../../dist';

export default function() {
  return (
    <Button.Group>
      <Button color="blue" primary>
        确认
      </Button>
    </Button.Group>
  );
}
